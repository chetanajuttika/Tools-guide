1.go to the file open with git bash

2. git init

3. git add . 

4.git commit -m "Initial commit"


GitHub part :

create new repository (name)
uncheck the lists(readme , Addgitignore,Add license)
copy the link(http)

GITBASH part :

5. git remote add origin link(http)

6. git remote -v (fetch,push(it will show these options)

7. git push origin master

IF YOU UPDATED A FILE :

1.git init
 
2.git status (it will show you the modified file)

3.git add . 

4.git commit -m "Updated Git notes"

5.git push -u origin master(if you are doing it for first time you have to do this from the the next time you can directly use git push command )

6.git push




